package useCases;

import java.util.List;

import backEnd.Attribute.AttributeImpl;
import backEnd.GameData.State.ComponentImpl;
import backEnd.GameData.State.State;
import main.ControllerImpl;

/**
 * This class shows how we will upgrade a component on the screen (ex: upgrade a tower's defensive abilities).
 */

/*

public class UpgradeComponent {
	Controller myController=new Controller();
	private State myState;
	
	List<Structure>[][] structGrid = myState.getStructureGrid();
	

	private Structure myStructure;
	
	public UpgradeComponent(Structure s, int i, int j, List<Attribute<?>> newAttributes){
		myStructure=s;
		modifyComponent(newAttributes, i, j);
	}
	
	public void modifyComponent(List<Attribute<?>> newAttributes, int i, int j){
		myStructure.replaceAttributes(newAttributes);
		myStructure.setLocation(i, j);
		
		myController.updateBackendAndFrontend();
		
	}
}
*/