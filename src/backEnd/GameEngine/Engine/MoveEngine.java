package backEnd.GameEngine.Engine;

import backEnd.GameData.State.State;

public class MoveEngine implements Engine{

	@Override
	public void gameLoop(State currentState) {
		
		for(int i=0; i<currentState.getGridWidth(); i++){
			for(int j=0; j<currentState.getGridWidth(); j++){
				currentState.getTileGrid().getAllComponents;
			}
		}
	}

}
