package backEnd.Bank;

public class BankController {
	//this essentially holds all the banks and has getters/setters for them
	//the individual banks should have getters/setters that take into account the access permissions
	
	//The Bank is accessed when the front end wants to be served options (Components, Options, Tiles, Attributes, Rules, etc.)
	//ModificationFromUser is access when the front end wants to make a change in the state
}
